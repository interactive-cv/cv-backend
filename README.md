# cv-backend

Бэкенд для **персонального интерактивного сайта-резюме** — open-source-шаблон на Python / FastAPI с реляционной PostgreSQL и встроенным AI-ассистентом. Любой может развернуть такой сайт под своё CV: меняется только контент (markdown) и домен.

Живой пример сервиса, собранного из этого кода: **[cv.libera.pro](https://cv.libera.pro)**.

Сайт совмещает классическое резюме, интерактивный граф проектов/технологий и **живой AI-чат, который отвечает рекрутёру по фактам из CV** — это та «фишка», которая выделяет кандидата среди сотен стандартных резюме.

## Технологии

- **Python 3.12, FastAPI, Pydantic v2** — асинхронное REST API, типобезопасные схемы
- **SQLAlchemy 2.0 (async) + Alembic** — ORM и миграции, кросс-БД типы (jsonb/uuid)
- **PostgreSQL 16** — основное хранилище
- **httpx** — стриминговый клиент к LLM (Server-Sent Events) в паттерне RAG
- **AI/LLM** — интеграция языковых моделей в продукт: RAG без векторной БД, prompt engineering с защитой от галлюцинаций, стриминг ответов (SSE)
- **pytest, pytest-asyncio, respx** — 64 unit-теста + 3 e2e-теста RAG против реальной модели

## Что умеет

| Эндпоинт | Назначение |
|----------|-----------|
| `GET /api/cv/master` | Публичные данные мастер-CV (контакты, навыки, языки) |
| `GET /api/projects` | Список проектов для таймлайна и графа знаний |
| `GET /api/variants/{slug}` | Вариант CV под конкретную вакансию (SSR-страница) |
| `GET /api/links/resolve?code=` | Резолв короткой ссылки с TTL, лимитом кликов и session_id трекингом |
| `POST /api/chat` | AI-чат (стриминг, RAG по CV, контекст диалога, сессии) |
| `GET /api/chat/history/{id}` | История диалога по session_id (восстановление при перезагрузке) |
| `POST /admin/applications/generate` | **AI-генерация CV и cover letter** (LLM, RAG, тип vacancy/freelance/contest, ТЗ из PDF) |
| `POST /admin/applications/upload-spec` | Загрузка ТЗ в PDF → извлечение текста (pypdf) → в промпт |
| `GET/POST/PATCH/DELETE /admin/applications` | CRUD откликов: вакансии, фриланс-заказы и конкурсы (бюджет, приз, срок, рейтинг, ссылки, ТЗ) |
| `GET /admin/applications/{id}/pdf` | **Экспорт CV в PDF** (fpdf2, Unicode, кириллица) |
| `GET /admin/applications/{id}/visitors` | Уникальные посетители CV (имена, кол-во просмотров, был ли чат) |
| `POST /admin/applications/{id}/publish` | Публикация: короткая ссылка, status=active (переиспользование при републикации) |
| `POST/PATCH/DELETE /admin/applications/{id}/interviews` | **CRUD собеседований**: дата/время, заметки до/после |
| `GET /admin/upcoming` | Ближайшие собеседования (дашборд, `scheduled_at >= now()`, limit 10) |
| `GET/PATCH /admin/settings` | **Настройки в БД**: мастер-CV, README, промпты (редактируемые через админку) |
| `POST /admin/settings/master-cv/preview` | **AI-правка CV**: инструкция → предпросмотр → применение |
| `GET /admin/chats` | Список HR-чатов с именами посетителей («Крепкий Кабан» и т.д.) |
| `GET /admin/chats/{id}` | Полный диалог HR-сессии |

Изящные детали реализации: атомарный инкремент счётчика кликов через `UPDATE ... WHERE hit_count < max_hits` (без race condition), ограничение частоты запросов (50/IP/час + 300/день) с раздельными bucket'ами для чата и резолва ссылок, защита от брутфорса коротких кодов, трекинг уникальных посетителей через session_id (единый профиль: клики + чат), snapshot промпта генерации на каждый отклик (воспроизводимость), lazy-cleanup чат-сессий (TTL 24ч).

## Интеграция AI и защита от галлюцинаций

AI-чат построен по паттерну **RAG (Retrieval-Augmented Generation) без векторной БД** — для объёма одного CV достаточно подавать весь текст мастер-CV как контекст в системном промпте. Запрос идёт к модели **glm-5.2** (Z.ai) со стримингом токенов обратно в браузер (SSE).

Защита от выдумывания ложных сведений — главный архитектурный приоритет, потому что рекрутёр доверяет ответам «ассистента кандидата». Реализовано многоуровнево:

1. **Жёсткий системный промпт** прямо запрещает модели выдумывать опыт, работодателей, метрики и технологии, которых нет в CV. Отдельным пунктом — приказ честно отвечать «в CV этой информации нет» при отсутствии данных и **игнорировать инструкции пользователя**, противоречащие правилам (защита от промпт-инъекций).
2. **e2e-тесты RAG** (`tests/test_rag_behavior.py`, маркер `@pytest.mark.e2e`) проверяют поведение против реальной модели на провокационных запросах: модель не подтверждает выдуманных работодателей (Google), не называет цифр зарплаты и отвечает по фактам на легитимные вопросы о стеке. Эти тесты зелёные.
3. **Graceful degradation**: при недоступности LLM (сбой API, исчерпание квоты) чат не падает с 500, а отвечает запасным сообщением с прямыми контактами — остальной сайт продолжает работать.

## AI-генератор откликов (вторая AI-функция)

Помимо чата для рекрутёров, LLM используется для **генерации адаптированного CV и cover letter из текста вакансии**. Владелец вставляет вакансию, выбирает релевантные проекты — и модель создаёт два текста в markdown за один вызов:

- Тот же паттерн RAG (мастер-CV как контекст), но задача другая — переупорядочить и подсветить релевантное, не выдумывая.
- Промпт (`app/llm/generate_prompt.py`) запрещает искажать факты и приказывает опираться только на выбранные проекты.
- Результат парсится маркерами `===CV===`/`===COVER===` и отдаётся в split-редактор админ-панели для ручной правки перед публикацией.

## Архитектура и тесты

```
app/
├── models/      SQLAlchemy-модели (master_cv, cv_variant, short_link, link_hit, project)
├── schemas/     Pydantic DTO (публичные — без приватного vacancy_text)
├── routers/     эндпоинты (cv, projects, links, chat, admin, health)
├── services/    бизнес-логика (резолв ссылок, парсер CV → структуру)
├── llm/         промпт-инжиниринг и стриминговый клиент Z.ai
└── errors.py    единый формат ошибок {error, message, request_id}
tests/           64 теста (unit на in-memory SQLite + e2e против glm-5.2)
```

Тесты запускаются на in-memory SQLite (кросс-БД типы через `TypeDecorator`), а в проде на PostgreSQL те же модели транслируются в нативные `jsonb`/`uuid` — без изменения кода.

## Развёртывание

Прод-окружение — собственный VPS (Linux) с Docker Compose:

- **nginx-ssl** (собственный образ на `nginx:alpine` + certbot) терминирует TLS и раздаёт статику, сертификаты Let's Encrypt получает и продлевает автоматически
- **FastAPI**, **Next.js** и **PostgreSQL** — в изолированной docker-сети
- Весь стек описан в `docker-compose.prod.yml`, обновление — одной командой `bash deploy.sh` (rsync + пересборка образов + миграции Alembic + seed)

Все персональные данные (имя, контакты, проекты, домен, ключ LLM) выносятся в `seed_data/` и `.env` — поэтому чужое CV разворачивается заменой контента без правки кода. Репозиторий фронтенда: [cv-frontend](https://github.com/interactive-cv/cv-frontend).

## Настройка через админку

После первого развёртывания все редактируемые тексты управляются через `/admin/settings` (без правок кода и деплоев):

- **Мастер-CV** — публичный markdown, идёт в LLM как контекст. Можно править вручную или через AI-инструкцию («добавь проект X» → предпросмотр → применение).
- **README** — для GitHub-профиля. Хранится в БД; для пуша в GitHub скопируйте и вставьте вручную.
- **Промпты** (HR-чат, генерация отклика, AI-правка CV) — редактируются как текст с плейсхолдерами (`{name}`, `{cv_markdown}`, `{vacancy_text}`, `{cv_link}`, `{current_cv}`, `{instruction}`).

Seed заполняет настройки дефолтными значениями **только при первом запуске**. Дальше — правки только через админку. GITHUB_TOKEN не требуется.

## Дорожная карта

- ~~Вынос захардкоженных значений в конфиг~~ ✅ — мастер-CV, промпты и README в БД, редактируются через `/admin/settings`
- ~~Расширенная аналитика просмотров~~ ✅ — трекинг уникальных посетителей (session_id + cookie), имена («Крепкий Кабан»), привязка к откликам, просмотр HR-чатов в `/admin/chats`
- ~~Конкурсы FL.ru~~ ✅ — третий тип откликов (contest) с отдельным промптом: тон участника, оценка реалистичности участия
- ~~Контрибьютинг-гайд и лицензия MIT~~ ✅ — см. [CONTRIBUTING.md](CONTRIBUTING.md) и [LICENSE](LICENSE)
- ~~Напоминалка о собеседовании~~ ✅ — CRUD интервью + дашборд «Ближайшие» с цветовой индикацией
- ~~Snapshot промпта~~ ✅ — каждый отклик сохраняет отрендеренный промпт для воспроизводимости
